version: '3.5'
services:
  nginx:
    image: "devsadds/nginx:1.20.0-alpine-libressl"
    build: 
      context: build/nginx/.
      args:
        APP_VERSION: "1.20.0"
        BASE_IMAGE: "alpine:3.12.1"
        BUILD_DATE: "$BUILD_DATE"
        BUIL_ARG_1: "http2"
